{
  "scripts": {
    "lint": "tslint --format stylish --project tsconfig.json"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "devDependencies": {
    "lerna": "^6.0.3",
    "prettier": "^2.0.5"
  }
}
